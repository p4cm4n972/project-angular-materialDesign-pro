import { AlphaListPipe } from './alpha-list.pipe';

describe('AlphaListPipe', () => {
  it('create an instance', () => {
    const pipe = new AlphaListPipe();
    expect(pipe).toBeTruthy();
  });
});
